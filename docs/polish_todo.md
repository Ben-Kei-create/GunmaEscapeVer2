# 群馬異界 磨き込みTODO

このメモは「大改修ではなく、遊び味と世界観の密度を上げる細部改善」を集めたもの。
方針は以下の3つで固定する。

- 土地への敬意がにじむこと
- 変な群馬ネタが、切なさに裏打ちされていること
- ルール説明がメタすぎず、主人公の気づきや環境の手触りで伝わること

## 今回着手済み

- [x] 開幕で最初から `アカギ` を名乗らせず、「見知らぬ案内人」から自然に正体が明かされる導線へ修正
- [x] 前橋初戦で、主人公の内面モノローグとしてダイス戦闘の読み方を補強
- [x] 戦果画面に `残響` を追加し、倒した相手の誇りや悲哀が一言残るようにした
- [x] 戦闘オーバーレイ会話を手動で早送りできるようにし、表示中は誤入力で行動確定しないよう調整
- [x] 前橋 / 高崎 / 下仁田 / 富岡 に環境ストーリーテリング用オブジェクトを追加
- [x] GAME OVER 画面に `つづきから` と `あいことば` の復帰導線を表示

## 優先度A: すぐ効く物語と導線

- [ ] 前橋開始直後に `主人公がまだ自分の名前を思い出せない` ことを、会話以外でも分かる短い独白を1回だけ追加する
- [ ] アカギ加入直後に `なぜこの男が旅人に肩入れするのか` を半歩だけ見せる短い追い会話を入れる
- [ ] 高崎クリア後、片目のだるまが少し減るなど、町の空気が変わったことを見た目で返す
- [ ] 下仁田クリア後、荷札や木箱の文言が `止まった輸送` から `再び運ばれる気配` に少し変わる
- [ ] 富岡クリア後、糸繰りの残響の周囲で機械音が止み、環境が静かになったことを一目で分かるようにする
- [ ] 主要町の看板や宿場オブジェクトを `次の土地の感情テーマ` につながる文言へ調整する
- [ ] 旅の途中で見つかるワゴン車や仲間の痕跡を、文字だけでなくオブジェクト配置で前倒し提示する
- [ ] 各章の入口で `この章で何を学ぶか` を、主人公の違和感として1行で言わせる

## 優先度A: 戦闘の理解と余韻

- [ ] 通常戦闘でも `この敵は何を守ろうとしているのか` が一瞬で伝わる短い意図表示を敵名の近くへ入れる
- [ ] ダイス停止時に `高い目を狙う戦い` と `低い目を使う儀式` の違いが分かる色やSE差分を追加する
- [ ] 儀式戦で正しい手順に近づいた瞬間、UIではなく場そのものが少し静まる演出を足す
- [ ] 敗北時に `なぜ今回はだめだったか` を1行で返す戦闘別ヒントを出す
- [ ] 仲間支援ログを結果画面だけでなく、発動したターンにも小さく光らせる
- [ ] ボス戦の会話オーバーレイは `自動 / 手動` の両方で読みやすいテンポに統一する
- [ ] 群れ戦に `先頭だけ気勢を上げる` `一体倒れると残りが怯む` などの隊列らしさを足す
- [ ] 戦果画面に `その土地の空気がどう変わったか` を返す短い町側コメントを追加する

## 優先度B: 環境ストーリーテリング

- [ ] 前橋に `見捨てられたコンビニ` `新幹線だけ通る遠景` など、青写真に沿った無言オブジェクトを足す
- [ ] 高崎に `剥げただるまの山` と `願掛け札の風化` を追加して、願いの町の切なさを濃くする
- [ ] 下仁田に `ねぎ箱` `峠道の運送痕` を置き、物流だった怪異の出自を視覚化する
- [ ] 富岡に `白い蜘蛛の巣に覆われたポスター` `破れた布` を追加し、止まった近代化を見せる
- [ ] 主要町のBGM以外に、環境音レイヤーをオブジェクト検分時だけ一瞬濃くする
- [ ] 同じ場所でも章クリア前後で見た目や文言が変わる `状態付きオブジェクト` を作る

## 優先度B: UI / UX

- [ ] フィールドメニューの `目標` を、現在地に応じてもう少し具体的な次の一歩へ寄せる
- [ ] ステータス画面で `仲間が何を支援しているか` をロール名だけでなく短文で示す
- [ ] `せってい` にイベント文字速度だけでなく `戦闘会話速度` も追加する
- [ ] タイトルの `つづきから` に最新記録の場所名を薄く添えて、戻り先を想像しやすくする
- [ ] あいことば生成後に `記録したか確認する` ためのワンクッションを入れる
- [ ] GAME OVER から `つづきから` へ直接行ける導線を追加するか検討する

## 優先度B: 音と手触り

- [ ] 高崎 / 下仁田 / 富岡で、章クリア後に環境音のニュアンスを少し変える
- [ ] 正しい儀式行動にだけ、土地ごとの固有SEを鳴らす
- [ ] だるま / 糸 / 湯 / 風など、章ごとの記号がUI点滅にも反映されるようにする
- [ ] マップ切替時のエリアバナーに、ごく短い環境SEを足す
- [ ] 町ごとの足音差分を入れて、道・土・板の違いを感じさせる

## 優先度C: システムと整頓

- [ ] `event.js` と `story.js` の旧導線を段階的に整理し、現在の旅路構造へ寄せる
- [ ] 旧4章構成の残骸会話を洗い出し、6章+終章の目的文とズレないようにする
- [ ] `render_game_to_text` に町側の状態変化や環境物会話の検証情報を少し足す
- [ ] デバッグ起動で `町ごとの環境物` と `戦果画面` をすぐ見られるショートカットを作る
- [ ] `progress.md` に章別の未接続イベント一覧を作り、物理削除候補を明示する
